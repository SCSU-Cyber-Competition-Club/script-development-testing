# AIO Hardening Script - Default Configuration
# Edit this before running Phase 1

# Competition Information
COMPETITION_NAME="MWCCDC Regional"
TEAM_NUMBER=""

# Role Selection (will be auto-detected in Phase 2, can pre-set here)
# Options: ecomm, webmail, splunk, custom
ROLE=""

# TCP Ports (comma-separated, will be auto-set based on role)
PORTS_TCP=""

# Splunk Configuration (for Phase 3)
SPLUNK_INDEXER_IP=""

# Logging
LOG_DIR="/var/log/aio_hardening"
REDFLAG_DIR="/var/log/redflags"

# Backup
BACKUP_DIR="/var/backups/aio"
BACKUP_RETENTION_DAYS=7

# Phase 1 Preferences
# Firewall mode: full_blackout, inbound_only, skip
# Leave empty to prompt during execution
FIREWALL_MODE=""

# SSH removal: yes, no
# Leave empty to prompt
REMOVE_SSH_PACKAGE=""

# NOTE: Phase 1 is fully interactive by design for safety
# Most options will prompt during execution
